---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shiny-dashboard
  labels:
    app: shiny-dashboard
    app.kubernetes.io/name: shiny-dashboard
    app.kubernetes.io/component: dashboard
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: shiny-dashboard
  template:
    metadata:
      labels:
        app: shiny-dashboard
        app.kubernetes.io/name: shiny-dashboard
    spec:
      containers:
      - name: shiny
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.service.targetPort }}
          name: http
          protocol: TCP
        env:
        - name: METRICS_URL
          value: {{ .Values.metrics.url | quote }}
        - name: REFRESH_INTERVAL
          value: {{ .Values.metrics.refreshInterval | quote }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /
            port: {{ .Values.service.targetPort }}
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: {{ .Values.service.targetPort }}
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: shiny-dashboard
  labels:
    app: shiny-dashboard
    app.kubernetes.io/name: shiny-dashboard
    app.kubernetes.io/component: dashboard
spec:
  selector:
    app: shiny-dashboard
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
    name: http
  type: ClusterIP
---
{{- if .Values.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: shiny-dashboard
  labels:
    app: shiny-dashboard
    app.kubernetes.io/name: shiny-dashboard
    app.kubernetes.io/component: dashboard
spec:
  {{- if .Values.route.host }}
  host: {{ .Values.route.host }}
  {{- end }}
  to:
    kind: Service
    name: shiny-dashboard
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
{{- end }}
